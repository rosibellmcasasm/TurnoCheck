// Destino del CTA: Preview anónimo -> paywall -> login (02C/ESTADO.md, default del SO) —
// nunca al checkout de Hotmart directo. El registro se pide DESPUÉS del onboarding, en el paywall.
export const CTA_HREF = "/onboarding";
